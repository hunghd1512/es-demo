#cluster.name: "docker-cluster"
network.host: 0.0.0.0

#discovery.seed_hosts: ["127.0.0.1", "192.168.1.11"]  # Liệt kê các node trong cluster
#cluster.initial_master_nodes: ["node_1", "node_2"]  # Đảm bảo rằng cả hai node được biết đến ngay từ đầu
#singe-node thì bỏ đoạn này

# Đường dẫn lưu trữ dữ liệu và logs
#path.data: /var/lib/elasticsearch
#path.logs: /var/log/elasticsearch
# Cấu hình loại node
#node.master: true
#node.data: true
#node.ingest: false
# Cổng HTTP (REST API)
http.port: 9200

# Cổng Transport (giao tiếp giữa các node)
#transport.port: 9300


#----------------------- BEGIN SECURITY AUTO CONFIGURATION -----------------------
#
# The following settings, TLS certificates, and keys have been automatically
# generated to configure Elasticsearch security features on 21-11-2022 18:57:26
#
# --------------------------------------------------------------------------------

# Enable security features
xpack.security.enabled: false
#xpack.security.enrollment.enabled: true

# Enable encryption for HTTP API client connections, such as Kibana, Logstash, and Agents

# Enable encryption and mutual authentication between cluster nodes
#xpack.license.self_generated.type: ${LICENSE}
# Create a new cluster with the current node only
# Additional nodes can still join the cluster later
#----------------------- END SECURITY AUTO CONFIGURATION -------------------------


